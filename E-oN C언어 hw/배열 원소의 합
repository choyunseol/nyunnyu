#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int array[6], i, count=0;
	for (i = 0; i <= 5; i++) {
		printf("%d %s", i, "번째 요소를 입력하세요");
		scanf("%d", &array[i]);
		count += array[i];
	}
	printf("%d", count);

	return 0;
}
